{% include 'post' %}

<footer class="post-responses">
  {% if post.response_count > 0 %}
    <div class="responses">
      <h2>{{ post.response_count | pluralize: 'Response' }}</h2>
      {% for response in post.responses %}
        {% include 'response' %}
      {% endfor %}
      {% if post.anon_upvote_count > 0 %}
        <div class="response">
          <div class="upvote">
            {{ post.anon_upvote_count | pluralize: 'visitor' }}
            upvoted this post.
          </div>
        </div>
      {% endif %}
    </div>
  {% endif %}

  {% if site.comments_enabled? %}
    <div class="new-comment">
      <h2>Add a Comment</h2>
      {% form 'comment' %}
    </div>
  {% endif %}
</footer>
