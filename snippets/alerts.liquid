{% unless posthaven.alerts == empty %}
  <div class='message'>
    {% for alert in posthaven.alerts %}
      <div class="alert alert-{{ alert.type }}">
        {{ alert.message }}
      </div>
    {% endfor %}
  </div>
{% endunless %}
